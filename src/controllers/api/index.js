const deleteImage = require('./deleteImage');
const getImage = require('./getImage');
const getList = require('./getList');
const uploadImage = require('./upload');
const replaceBackground = require('./replaceBackground');

module.exports = {
    deleteImage,
    getList,
    getImage,
    uploadImage,
    replaceBackground
}